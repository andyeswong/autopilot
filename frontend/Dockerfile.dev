FROM node:22-alpine

WORKDIR /app

VOLUME [ "/app" ]

# IMPORTANT:
# Vite uses esbuild under the hood, which is a native dependency and must be
# compiled inside the container for the target Linux platform.

ENTRYPOINT [ "sh", "-c", "npm install && npm run dev" ]